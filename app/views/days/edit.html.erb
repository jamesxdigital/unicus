<div class="col-xs-12 col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
  <h1 class="page-header">Edit a day</h1>
<%= form_for :day, url: day_path(@day), method: :patch do |f| %>
  <% if @day.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@days.errors.count, "error") %> prohibited
      this day from being saved:</h2>
    <ul>
    <% @day.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

        <form>
          <div class="form-group">
            <%=label_tag "Type date (dd/mm/yyyy)" %><br>
            <%= f.text_field :date, :class => "form-control", :placeholder => "Example: 30-07-1995" %>
          </div>
          <div class="form-group">
            <%= label_tag "Select Day" %><br>
            <%= f.select(:name, [['Monday'], ['Tuesday'],['Wednesday'],['Thursday'],['Friday'],['Saturday'],['Sunday']]) %>
          </div>
          <div class="form-group">
            <%= label_tag "Display size" %><br>
            <%= f.select(:display_size, [['6'], ['12']]) %>
          </div>
          <%=submit_tag "Save changes", :class=>"btn btn-default" %>
          <%= link_to 'Cancel', days_path, :class=>"btn btn-default" %>
        </form>
      </div>
    </div>
  <% end %>
